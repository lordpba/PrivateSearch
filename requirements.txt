# PrivateSearch — Python dependencies
# Lightweight container: NO GPU dependencies, NO PyTorch, NO CUDA
# All AI inference is handled by Ollama running on the host

# Web UI
gradio>=5.0,<6.0

# Vector store
chromadb>=0.5,<1.0

# HTTP client (for Ollama API calls)
httpx>=0.27,<1.0

# Document processing
PyMuPDF>=1.24,<2.0          # PDF text extraction + rendering
python-docx>=1.1,<2.0       # DOCX extraction
pandas>=2.1,<3.0             # CSV processing
Pillow>=10.0,<11.0           # Image handling

# Hybrid search (BM25 keyword index alongside semantic search)
rank-bm25>=0.2,<1.0

cryptography